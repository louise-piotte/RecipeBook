<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeBook">
        <activity
            android:name=".CollectionManagerActivity"
            android:exported="false"
            android:label="@string/manage_collections_label"
            android:theme="@style/Theme.RecipeBook" />
        <activity
            android:name=".IngredientTagManagerActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/manage_library_data_short_label"
            android:theme="@style/Theme.RecipeBook" />
        <activity
            android:name=".RecipeEditorActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/edit_recipe_label"
            android:theme="@style/Theme.RecipeBook" />
        <activity
            android:name=".RecipeDetailActivity"
            android:exported="false"
            android:label="@string/detail_section_title"
            android:theme="@style/Theme.RecipeBook" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/recipe_photo_filepaths" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/app_name"
            android:theme="@style/Theme.RecipeBook">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
